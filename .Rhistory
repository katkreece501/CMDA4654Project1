all_spans <- rbind(test_model_df, test_model_df2)
spans1 <- filter(all_spans, degee == 1)
all_spans <- rbind(test_model_df, test_model_df2)
spans1 <- filter(all_spans, degree == 1)
spans2 <- filter(all_spans, degree == 2)
span_curve1 <- ggplot(data = spans1, aes(x = span, y = MSE)) + geom_point() +
geom_line(aes(group = 1)) + theme_bw() + labs(title = "degree = 1")
span_curve2 <- ggplot(data = spans2, aes(x = span, y = MSE)) + geom_point() +
geom_line(aes(group = 1)) + theme_bw() + labs(title = "degree = 2")
span_curve1 + span_curve2 + plot_annotation(title = "Span vs MSE")
source("~/CMDA4654/datasettest.R")
View(fgl)
var_names <- names(fgl)
var_types <- c("numeric", "numeric", "numeric", "numeric", "numeric", "numeric",
"numeric", "numeric", "numeric", "Factor")
var_labels <- c("Last 4 digits of refractive index",
"Sodium (% by weight of oxide)",
"Manganese (% by weight of oxide)",
"Aluminum (% by weight of oxide)",
"Silicon (% by weight of oxide)",
"Potassium (% by weight of oxide)",
"Calcium (% by weight of oxide)",
"Barium (% by weight of oxide)",
"Iron (% by weight of oxide)",
"Glass type, classified by origin")
data_dict <- data.frame(Name = var_names,
Description = var_labels,
Type = var_types,
stringsAsFactors = FALSE)
knitr::kable(
data_dict,
caption = "Data dictionary for fgl dataset"
)
# Scatterplot of Ca vs Mg
ggplot(data = fgl, aes(x = Mg, y = Ca)) + geom_point() + theme_bw() +
labs(title = "Calcium vs Manganese % by weight of oxide in glass fragments",
x = "Manganese", y = "Calcium")
library(lars)
install.packages("lars")
# For reproducibility
set.seed(329)
k <- 10
spans <- seq(0.1, 0.5, by = 0.01)
folds <- split(sample(1:214), rep(1:k, length = 214))
MSE_d1 <- matrix(NA, nrow = length(folds), ncol = length(spans) )
MSE_d2 <- matrix(NA, nrow = length(folds), ncol = length(spans) )
colnames(MSE_d1) <- spans
colnames(MSE_d2) <- spans
for ( i in 1:length(folds) ){
testing_indices <- folds[[i]]
training_data <- fgl[-testing_indices, c("Mg", "Ca")]
testing_data <- fgl[testing_indices, c("Mg", "Ca")]
for (j in 1:length(spans)) {
fit1 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans[j], degree = 1)
MSE_d1[i, j] <- fit1$MSE
fit2 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans[j], degree = 2)
MSE_d2[i, j] <- fit2$MSE
}
}
# For reproducibility
set.seed(329)
k <- 10
spans <- seq(0.15, 0.5, by = 0.01)
folds <- split(sample(1:214), rep(1:k, length = 214))
MSE_d1 <- matrix(NA, nrow = length(folds), ncol = length(spans) )
MSE_d2 <- matrix(NA, nrow = length(folds), ncol = length(spans) )
colnames(MSE_d1) <- spans
colnames(MSE_d2) <- spans
for ( i in 1:length(folds) ){
testing_indices <- folds[[i]]
training_data <- fgl[-testing_indices, c("Mg", "Ca")]
testing_data <- fgl[testing_indices, c("Mg", "Ca")]
for (j in 1:length(spans)) {
fit1 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans[j], degree = 1)
MSE_d1[i, j] <- fit1$MSE
fit2 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans[j], degree = 2)
MSE_d2[i, j] <- fit2$MSE
}
}
# For reproducibility
set.seed(329)
k <- 10
spans <- seq(0.2, 0.5, by = 0.01)
folds <- split(sample(1:214), rep(1:k, length = 214))
MSE_d1 <- matrix(NA, nrow = length(folds), ncol = length(spans) )
MSE_d2 <- matrix(NA, nrow = length(folds), ncol = length(spans) )
colnames(MSE_d1) <- spans
colnames(MSE_d2) <- spans
for ( i in 1:length(folds) ){
testing_indices <- folds[[i]]
training_data <- fgl[-testing_indices, c("Mg", "Ca")]
testing_data <- fgl[testing_indices, c("Mg", "Ca")]
for (j in 1:length(spans)) {
fit1 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans[j], degree = 1)
MSE_d1[i, j] <- fit1$MSE
fit2 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans[j], degree = 2)
MSE_d2[i, j] <- fit2$MSE
}
}
# For reproducibility
set.seed(329)
k <- 10
spans <- seq(0.3, 0.7, by = 0.01)
folds <- split(sample(1:214), rep(1:k, length = 214))
MSE_d1 <- matrix(NA, nrow = length(folds), ncol = length(spans) )
MSE_d2 <- matrix(NA, nrow = length(folds), ncol = length(spans) )
colnames(MSE_d1) <- spans
colnames(MSE_d2) <- spans
for ( i in 1:length(folds) ){
testing_indices <- folds[[i]]
training_data <- fgl[-testing_indices, c("Mg", "Ca")]
testing_data <- fgl[testing_indices, c("Mg", "Ca")]
for (j in 1:length(spans)) {
fit1 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans[j], degree = 1, show.plot = FALSE)
MSE_d1[i, j] <- fit1$MSE
fit2 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans[j], degree = 2, show.plot = FALSE)
MSE_d2[i, j] <- fit2$MSE
}
}
View(test_models2)
View(MSE_d1)
colMeans(MSE_d1)
colMeans(MSE_d2)
spans[which.min(colMeans(MSE_d1))]
spans[which.min(colMeans(MSE_d2))]
# For reproducibility
set.seed(329)
k <- 10
spans <- seq(0.25, 0.7, by = 0.01)
folds <- split(sample(1:214), rep(1:k, length = 214))
MSE_d1 <- matrix(NA, nrow = length(folds), ncol = length(spans) )
MSE_d2 <- matrix(NA, nrow = length(folds), ncol = length(spans) )
colnames(MSE_d1) <- spans
colnames(MSE_d2) <- spans
for ( i in 1:length(folds) ){
testing_indices <- folds[[i]]
training_data <- fgl[-testing_indices, c("Mg", "Ca")]
testing_data <- fgl[testing_indices, c("Mg", "Ca")]
for (j in 1:length(spans)) {
fit1 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans[j], degree = 1, show.plot = FALSE)
MSE_d1[i, j] <- fit1$MSE
fit2 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans[j], degree = 2, show.plot = FALSE)
MSE_d2[i, j] <- fit2$MSE
}
}
spans[which.min(colMeans(MSE_d1))]
spans[which.min(colMeans(MSE_d2))]
# For reproducibility
set.seed(329)
k <- 10
spans1 <- seq(0.20, 0.60, by = 0.01)
spans2 <- seq(0.30, 0.70, by = 0.01)
folds <- split(sample(1:214), rep(1:k, length = 214))
MSE_d1 <- matrix(NA, nrow = length(folds), ncol = length(spans) )
MSE_d2 <- matrix(NA, nrow = length(folds), ncol = length(spans) )
colnames(MSE_d1) <- spans1
# For reproducibility
set.seed(329)
k <- 10
spans1 <- seq(0.20, 0.60, by = 0.01)
spans2 <- seq(0.30, 0.70, by = 0.01)
folds <- split(sample(1:214), rep(1:k, length = 214))
MSE_d1 <- matrix(NA, nrow = length(folds), ncol = length(spans1) )
MSE_d2 <- matrix(NA, nrow = length(folds), ncol = length(spans2) )
colnames(MSE_d1) <- spans1
colnames(MSE_d2) <- spans2
for ( i in 1:length(folds) ){
testing_indices <- folds[[i]]
training_data <- fgl[-testing_indices, c("Mg", "Ca")]
testing_data <- fgl[testing_indices, c("Mg", "Ca")]
for (j in 1:length(spans)) {
fit1 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans1[j], degree = 1, show.plot = FALSE)
MSE_d1[i, j] <- fit1$MSE
fit2 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans2[j], degree = 2, show.plot = FALSE)
MSE_d2[i, j] <- fit2$MSE
}
}
rlang::last_trace()
# For reproducibility
set.seed(329)
k <- 10
spans1 <- seq(0.21, 0.60, by = 0.01)
spans2 <- seq(0.30, 0.70, by = 0.01)
folds <- split(sample(1:214), rep(1:k, length = 214))
MSE_d1 <- matrix(NA, nrow = length(folds), ncol = length(spans1) )
MSE_d2 <- matrix(NA, nrow = length(folds), ncol = length(spans2) )
colnames(MSE_d1) <- spans1
colnames(MSE_d2) <- spans2
for ( i in 1:length(folds) ){
testing_indices <- folds[[i]]
training_data <- fgl[-testing_indices, c("Mg", "Ca")]
testing_data <- fgl[testing_indices, c("Mg", "Ca")]
for (j in 1:length(spans)) {
fit1 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans1[j], degree = 1, show.plot = FALSE)
MSE_d1[i, j] <- fit1$MSE
fit2 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans2[j], degree = 2, show.plot = FALSE)
MSE_d2[i, j] <- fit2$MSE
}
}
rlang::last_trace()
rlang::last_trace(drop = FALSE)
# For reproducibility
set.seed(329)
k <- 10
spans1 <- seq(0.22, 0.60, by = 0.01)
spans2 <- seq(0.32, 0.70, by = 0.01)
folds <- split(sample(1:214), rep(1:k, length = 214))
MSE_d1 <- matrix(NA, nrow = length(folds), ncol = length(spans1) )
MSE_d2 <- matrix(NA, nrow = length(folds), ncol = length(spans2) )
colnames(MSE_d1) <- spans1
colnames(MSE_d2) <- spans2
for ( i in 1:length(folds) ){
testing_indices <- folds[[i]]
training_data <- fgl[-testing_indices, c("Mg", "Ca")]
testing_data <- fgl[testing_indices, c("Mg", "Ca")]
for (j in 1:length(spans)) {
fit1 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans1[j], degree = 1, show.plot = FALSE)
MSE_d1[i, j] <- fit1$MSE
fit2 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans2[j], degree = 2, show.plot = FALSE)
MSE_d2[i, j] <- fit2$MSE
}
}
View(MSE_d2)
# For reproducibility
set.seed(329)
k <- 10
spans1 <- seq(0.23, 0.60, by = 0.01)
#spans2 <- seq(0.32, 0.70, by = 0.01)
folds <- split(sample(1:214), rep(1:k, length = 214))
MSE_d1 <- matrix(NA, nrow = length(folds), ncol = length(spans1) )
#MSE_d2 <- matrix(NA, nrow = length(folds), ncol = length(spans2) )
colnames(MSE_d1) <- spans1
#colnames(MSE_d2) <- spans2
for ( i in 1:length(folds) ){
testing_indices <- folds[[i]]
training_data <- fgl[-testing_indices, c("Mg", "Ca")]
testing_data <- fgl[testing_indices, c("Mg", "Ca")]
for (j in 1:length(spans)) {
fit1 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans1[j], degree = 1, show.plot = FALSE)
MSE_d1[i, j] <- fit1$MSE
#fit2 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
#x_test = testing_data$Mg, y_test = testing_data$Ca,
#span = spans2[j], degree = 2, show.plot = FALSE)
#MSE_d2[i, j] <- fit2$MSE
}
}
# For reproducibility
set.seed(329)
k <- 10
spans1 <- seq(0.24, 0.60, by = 0.01)
#spans2 <- seq(0.32, 0.70, by = 0.01)
folds <- split(sample(1:214), rep(1:k, length = 214))
MSE_d1 <- matrix(NA, nrow = length(folds), ncol = length(spans1) )
#MSE_d2 <- matrix(NA, nrow = length(folds), ncol = length(spans2) )
colnames(MSE_d1) <- spans1
#colnames(MSE_d2) <- spans2
for ( i in 1:length(folds) ){
testing_indices <- folds[[i]]
training_data <- fgl[-testing_indices, c("Mg", "Ca")]
testing_data <- fgl[testing_indices, c("Mg", "Ca")]
for (j in 1:length(spans)) {
fit1 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans1[j], degree = 1, show.plot = FALSE)
MSE_d1[i, j] <- fit1$MSE
#fit2 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
#x_test = testing_data$Mg, y_test = testing_data$Ca,
#span = spans2[j], degree = 2, show.plot = FALSE)
#MSE_d2[i, j] <- fit2$MSE
}
}
# For reproducibility
set.seed(329)
k <- 10
spans1 <- seq(0.25, 0.60, by = 0.01)
#spans2 <- seq(0.32, 0.70, by = 0.01)
folds <- split(sample(1:214), rep(1:k, length = 214))
MSE_d1 <- matrix(NA, nrow = length(folds), ncol = length(spans1) )
#MSE_d2 <- matrix(NA, nrow = length(folds), ncol = length(spans2) )
colnames(MSE_d1) <- spans1
#colnames(MSE_d2) <- spans2
for ( i in 1:length(folds) ){
testing_indices <- folds[[i]]
training_data <- fgl[-testing_indices, c("Mg", "Ca")]
testing_data <- fgl[testing_indices, c("Mg", "Ca")]
for (j in 1:length(spans)) {
fit1 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans1[j], degree = 1, show.plot = FALSE)
MSE_d1[i, j] <- fit1$MSE
#fit2 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
#x_test = testing_data$Mg, y_test = testing_data$Ca,
#span = spans2[j], degree = 2, show.plot = FALSE)
#MSE_d2[i, j] <- fit2$MSE
}
}
# For reproducibility
set.seed(329)
k <- 10
spans1 <- seq(0.20, 0.60, by = 0.01)
spans2 <- seq(0.30, 0.70, by = 0.01)
folds <- split(sample(1:214), rep(1:k, length = 214))
MSE_d1 <- matrix(NA, nrow = k, ncol = length(spans1) )
MSE_d2 <- matrix(NA, nrow = length(folds), ncol = length(spans2) )
colnames(MSE_d1) <- spans1
#colnames(MSE_d2) <- spans2
for ( i in 1:k ){
testing_indices <- folds[[i]]
training_data <- fgl[-testing_indices, c("Mg", "Ca")]
testing_data <- fgl[testing_indices, c("Mg", "Ca")]
for (j in 1:length(spans1)) {
fit1 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans1[j], degree = 1, show.plot = FALSE)
MSE_d1[i, j] <- fit1$MSE
fit2 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
#x_test = testing_data$Mg, y_test = testing_data$Ca,
#span = spans2[j], degree = 2, show.plot = FALSE)
MSE_d2[i, j] <- fit2$MSE
}
# For reproducibility
set.seed(329)
k <- 10
spans1 <- seq(0.20, 0.60, by = 0.01)
spans2 <- seq(0.30, 0.70, by = 0.01)
folds <- split(sample(1:214), rep(1:k, length = 214))
MSE_d1 <- matrix(NA, nrow = k, ncol = length(spans1) )
MSE_d2 <- matrix(NA, nrow = length(folds), ncol = length(spans2) )
colnames(MSE_d1) <- spans1
#colnames(MSE_d2) <- spans2
for ( i in 1:k ){
testing_indices <- folds[[i]]
training_data <- fgl[-testing_indices, c("Mg", "Ca")]
testing_data <- fgl[testing_indices, c("Mg", "Ca")]
for (j in 1:length(spans1)) {
fit1 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans1[j], degree = 1, show.plot = FALSE)
MSE_d1[i, j] <- fit1$MSE
fit2 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans2[j], degree = 2, show.plot = FALSE)
MSE_d2[i, j] <- fit2$MSE
}
}
# For reproducibility
set.seed(329)
k <- 10
spans1 <- seq(0.21, 0.61, by = 0.01)
spans2 <- seq(0.31, 0.71, by = 0.01)
folds <- split(sample(1:214), rep(1:k, length = 214))
MSE_d1 <- matrix(NA, nrow = k, ncol = length(spans1) )
MSE_d2 <- matrix(NA, nrow = length(folds), ncol = length(spans2) )
colnames(MSE_d1) <- spans1
#colnames(MSE_d2) <- spans2
for ( i in 1:k ){
testing_indices <- folds[[i]]
training_data <- fgl[-testing_indices, c("Mg", "Ca")]
testing_data <- fgl[testing_indices, c("Mg", "Ca")]
for (j in 1:length(spans1)) {
fit1 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans1[j], degree = 1, show.plot = FALSE)
MSE_d1[i, j] <- fit1$MSE
fit2 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans2[j], degree = 2, show.plot = FALSE)
MSE_d2[i, j] <- fit2$MSE
}
}
spans[which.min(colMeans(MSE_d1))]
spans[which.min(colMeans(MSE_d1))]
spans[which.min(colMeans(MSE_d2))]
# For reproducibility
set.seed(329)
k <- 10
n <- nrow(fgl)
# Different span values for each degree, as to avoid errors where there
# aren't more than degree number of unique x-values in a window
spans_d1 <- seq(0.21, 0.60, by = 0.01)
spans_d2 <- seq(0.31, 0.70, by = 0.01)
# Make folds
folds <- split(sample(1:n), rep(1:k, length = n))
# Make MSE matrices, where each row corresponds to a fold
# and each column corresponds to a different span
MSE_d1 <- matrix(NA, nrow = k, ncol = length(spans_d1))
MSE_d2 <- matrix(NA, nrow = k, ncol = length(spans_d2))
# For each fold, separate out training and testing data
for (i in 1:k){
testing_indices <- folds[[i]]
training_data <- fgl[-testing_indices, c("Mg", "Ca")]
testing_data <- fgl[testing_indices, c("Mg", "Ca")]
# For each span, fit the model and record MSE
for (j in 1:length(spans_d1)) {
fit1 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans_d1[j], degree = 1, show.plot = FALSE)
MSE_d1[i, j] <- fit1$MSE
fit2 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans_d2[j], degree = 2, show.plot = FALSE)
MSE_d2[i, j] <- fit2$MSE
}
}
# Get mean MSEs for each span
mean_MSE_d1 <- colMeans(MSE_d1)
mean_MSE_d2 <- colMeans(MSE_d2)
# Find optimal span for each degree, based on which has the lowest
# corresponding average MSE across all folds
optim_span_d1 <- spans[which.min(mean_MSE_d1)]
optim_span_d2 <- spans[which.min(mean_MSE_d2)]
mean_MSE_d2
min(mean_MSE_d2)
mean_MSE_d2[0.69]
mean_MSE_d2[39]
mean_MSE_d2[38]
spans_d2[38]
optim_span_d2
which.min(mean_MSE_d2)
spans_d2[38]
spans1[which.min(mean_MSE_d1)]
# For reproducibility
set.seed(329)
k <- 10
n <- nrow(fgl)
# Different span values for each degree, as to avoid errors where there
# aren't more than degree number of unique x-values in a window
spans_d1 <- seq(0.21, 0.60, by = 0.01)
spans_d2 <- seq(0.31, 0.70, by = 0.01)
# Make folds
folds <- split(sample(1:n), rep(1:k, length = n))
# Make MSE matrices, where each row corresponds to a fold
# and each column corresponds to a different span
MSE_d1 <- matrix(NA, nrow = k, ncol = length(spans_d1))
MSE_d2 <- matrix(NA, nrow = k, ncol = length(spans_d2))
# For each fold, separate out training and testing data
for (i in 1:k){
testing_indices <- folds[[i]]
training_data <- fgl[-testing_indices, c("Mg", "Ca")]
testing_data <- fgl[testing_indices, c("Mg", "Ca")]
# For each span, fit the model and record MSE
for (j in 1:length(spans_d1)) {
fit1 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans_d1[j], degree = 1, show.plot = FALSE)
MSE_d1[i, j] <- fit1$MSE
fit2 <- myloess(x_train = training_data$Mg, y_train = training_data$Ca,
x_test = testing_data$Mg, y_test = testing_data$Ca,
span = spans_d2[j], degree = 2, show.plot = FALSE)
MSE_d2[i, j] <- fit2$MSE
}
}
# Get mean MSEs for each span
mean_MSE_d1 <- colMeans(MSE_d1)
mean_MSE_d2 <- colMeans(MSE_d2)
# Find optimal span for each degree, based on which has the lowest
# corresponding average MSE across all folds
optim_span_d1 <- spans1[which.min(mean_MSE_d1)]
optim_span_d2 <- spans2[which.min(mean_MSE_d2)]
mean_MSE_d1
spans_df1 <- data.frame(span = spans_d1, MSE = mean_MSE_d1)
spans_df2 <- data.frame(span = spans_d2, MSE = mean_MSE_d2)
span_curve_d1 <- ggplot(data = spans_df1, aes(x = span, y = MSE)) + geom_point() +
geom_line(aes(group = 1)) + theme_bw() + labs(title = "degree = 1")
span_curve_d2 <- ggplot(data = spans_df2, aes(x = span, y = MSE)) + geom_point() +
geom_line(aes(group = 1)) + theme_bw() + labs(title = "degree = 2")
span_curve_d1 + span_curve_d2 + plot_annotation(title = "MSE vs span")
optim_d1 <- myloess(x_train = fgl$Mg, y_train = fgl$Ca, span = 0.23, degree = 1)
optim_d2 <- myloess(x_train = fgl$Mg, y_train = fgl$Ca, span = 0.68, degree = 2)
optim_d1_plot <- optim_d1$loessplot + labs(title = "degree = 1, span = 0.23",
x = "Manganese", y = "Calcium")
optim_d2_plot <- optim_d2$loessplot + labs(title = "degree = 2, span = 0.68",
x = "Manganese", y = "Calcium")
optim_d1_plot + optim_d2_plot + plot_annotation(title = "Optimal LOESS fits, fgl data")
optim_span_d2 <- spans2[which.min(mean_MSE_d2)]
spans2[which.min(mean_MSE_d2)]
str(spans2)
class(spans2)
